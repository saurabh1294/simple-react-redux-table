import { combineReducers } from "redux";
import tableReducer from "./tableReducer";

const rootReducer = combineReducers({
    tableReducer
    // TODO add more reducers here as they come
});

export default rootReducer;